import styled from 'styled-components';

const Title = styled.h3`
    margin-top: 30px;
    color: #fff;
    font-style: normal;
    font-weight: bold;
    font-size: 24px;
    width: 70%;
`;

export default Title;
